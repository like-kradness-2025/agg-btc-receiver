# Worklog: burst-agg data verification (2026-07-07)

## Goal
binance_perp の burst-agg 出力データを検証し、正確性を確認する。

## Evidence collected so far
- 変換済みデータ: /tmp/burst-agg-1d/.staging/ (4 run, 33,693ファイル, 140MB)
- 期間: 2026-07-06 15:00〜23:00Z (8時間)
- 4 run の集計結果は確認済み

## 検証観点
1. 出力ファイルの構造（summary 22列、features 15列が正しく出力されているか）
2. burst カウントの妥当性（trade数に対する burst 比率）
3. 時系列の連続性（window の ts が飛び飛びになってないか）
4. burst_print_sizes の符号規則（buy=正, sell=負）
5. book state が正しく復元されているか（best_bid < best_ask）
6. 空 window が正しく出力されているか（trade 0 でも summary が出る）

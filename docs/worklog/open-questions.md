# Open Questions

## Non-blocking follow-up
- Add an explicit normative section for the four core burst 1s fields (`burst_count_1s`, `max_burst_notional_1s`, `max_burst_prints_1s`, `max_burst_duration_ms_1s`) in either Phase 1 or Phase 4 docs.
- Add a reconciliation note between pre-existing v2 trade-level at-touch/through columns and the new burst-level validation columns.

## Bridge-plan phase
- Freeze the minimal implementation spine before coding: existing accumulator reuse, responsibility split, first vertical slice, and verification package.

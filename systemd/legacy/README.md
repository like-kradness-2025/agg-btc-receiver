# Legacy units

The archive units belong to the pre-DuckDB JSONL/Parquet flow.
They are not enabled by the current Receiver policy. The maintenance unit files
remain at their historical paths because guardrail tests inspect those paths;
they are also disabled and are not part of the current Receiver service.
